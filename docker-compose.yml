version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:14-alpine
    container_name: docuflow-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: docprocessing
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # API Server (optional - uncomment to run API in Docker too)
  # api:
  #   build: .
  #   container_name: docuflow-api
  #   restart: unless-stopped
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - NODE_ENV=development
  #     - DATABASE_URL=postgresql://postgres:password@postgres:5432/docprocessing
  #     - DB_HOST=postgres
  #     - DB_PORT=5432
  #     - DB_NAME=docprocessing
  #     - DB_USER=postgres
  #     - DB_PASSWORD=password
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   volumes:
  #     - .:/app
  #     - /app/node_modules
  #   command: npm run dev

volumes:
  postgres_data:
    driver: local
